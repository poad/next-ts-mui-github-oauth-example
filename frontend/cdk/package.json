{
  "name": "@next-ts-mui-github-oauth-example/frontend-cdk",
  "version": "0.1.0",
  "bin": {
    "cognito": "bin/cloudfront-s3-cdn.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "eslint:check": "eslint ./{bin,lib}/**/*.ts",
    "eslint:fix": "eslint ./{bin,lib}/**/*.ts --fix",
    "style:check": "prettier --write bin lib",
    "style:fix": "prettier --write bin lib",
    "lint": "pnpm run --parallel /^.*:check$/",
    "lint-fix": "pnpm run /^.*:fix$/",
    "clean": "rimraf build",
    "prebuild": "pnpm lint"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "aws-cdk": "^2.136.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.4"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.136.1",
    "constructs": "^10.3.0",
    "esbuild": "^0.20.2",
    "source-map-support": "^0.5.21"
  },
  "engines": {
    "npm": "use pnpm please!",
    "yarn": "use pnpm please!",
    "pnpm": ">=8.7.4"
  }
}
